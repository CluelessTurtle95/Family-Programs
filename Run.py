import Helper

filename = 'user-input'
file = open(file , 'r')
arg = list()

for row in file:
    arg.append(row)

location = arg[0]
for index  , line in enumerate(arg)
    if index != 0:
        message += line + '\n' 

Helper.compareDescription(arg[0] , arg[1])
